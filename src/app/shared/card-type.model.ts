export class CardType {
    /**
     * Title of the card type.
     */
    title: string;

    /**
     * xwing-miniatures-font CSS class name used to display the icon.
     */
    iconClassName: string;

    /**
     * Default length of Caddy in millimeters.
     */
    defaultLength: number;
}